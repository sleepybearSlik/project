import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";
import LazyLoadedImage from "./LazyLoadImage";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <LazyLoadedImage
          src={icon}
          alt="web-development"
          className="w-30 h-30 object-contain"
          style={{borderRadius:'10px'}}
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-5xl leading-[30px]"
      >
        I'm a dynamic software developer proficient in TypeScript and
        JavaScript, showcasing expertise in cutting-edge libraries and run-time enviroment such as
        React and Node.js respectively. As a fast learner, I thrive on collaborating closely
        with clients to craft efficient, scalable, and delightful solutions that
        address real-world challenges. Let's team up and breathe life into your
        innovative ideas!
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10" style={{display:'flex', justifyContent:'center'}}>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
